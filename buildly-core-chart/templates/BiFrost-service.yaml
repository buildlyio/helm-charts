apiVersion: v1
kind: Service
metadata:
  name: bifrost
spec:
  ports:
  - name: "http"
    port: 8080
    targetPort: 8080
  selector:
    app: bifrost
    release: {{ .Release.Name }}
    releaseName: {{ .Release.Name }}
  type: NodePort
status:
  loadBalancer: {}
